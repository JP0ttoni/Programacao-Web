<nav>
    <div>
      <a href="/">home</a>
      <a href="{{ route('list_products') }}">Produtos</a>
      
      @if( !await auth.check())
      <a href="/login">login</a>
      <a href="/users/create_user"> criar usuario </a>
      @else
      <a href="/logout">logout</a>
      <a href="{{ route('edit_user') }}">editar perfil</a>
      <p>usuário: {{ auth.user?.username }}</p>
      @if(auth.user.username  == 'admin')
      <a href="{{ route('page_new_product') }}">Criar Produto</a>
      @endif
      
      @endif
      
    </div>
  </nav>