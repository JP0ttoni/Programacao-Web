<!DOCTYPE html>
<html>
  <head>
    <title>Site</title>  
  </head>
  <body>    
    <nav>
      <a>escreva apenas nos dados que deseja mudar</a>
    </nav>
    <main>
        <form id = 'form' action="{{ route('user_merge') }}" method="POST">
            <div>
                Nome Completo:<input type="text" name="fullName" placeholder="seu novo nome completo">
            </div>
            <div>
                Senha:<input type="password" name="password" placeholder="sua nova senha completa">
            </div>
            <div>
                Nome de usuário:<input type="text" name="username" placeholder="seu novo nome de usuário">
            </div>
            <div>
                <button type="submit">enviar</button>
            </div>
            <a href="/">cancelar</a>
        </form>

        <script>
            document.getElementById('form').addEventListener('keydown', function(event) {
                if (event.key === 'Enter') {
                    event.preventDefault();
                }
            })

        </script>
    </main>
  </body>
</html>